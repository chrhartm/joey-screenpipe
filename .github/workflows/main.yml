name: Main

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]

jobs:
    build:
        strategy:
            matrix:
                # os: [ubuntu-latest, macos-latest, windows-latest]
                os: [ubuntu-latest]
        uses: ./.github/workflows/build.yml
